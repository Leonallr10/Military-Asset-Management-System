# Backend API origin (no trailing slash).
# Local with Vite proxy: leave unset or empty (recommended for local register/login).
# Against deployed API (must include /api/bases/public — redeploy server first):
# VITE_API_URL=https://server-rho-khaki.vercel.app
VITE_API_URL=
